# Engine + clients + dashboard deps.
# Kept separate from pyproject.toml/uv.lock so the BAND agent env stays untouched.
#   python -m venv .venv-engine && . .venv-engine/bin/activate
#   pip install -r requirements-engine.txt

httpx>=0.27
python-dotenv>=1.0
google-genai>=1.0            # judge + embeddings (free tier)
actian-vectorai-client       # experience memory (needs the docker container running)
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
